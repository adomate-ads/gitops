apiVersion: v1
kind: Service
metadata:
  labels:
    run: site-analyzer
  name: site-analyzer
spec:
  ports:
  - port: 3000
    targetPort: 3000
  selector:
    run: site-analyzer
---
kind: Deployment
metadata:
  name: site-analyzer
  namespace: site-analyzer
site-analyzerVersion: apps/v1
spec:
  replicas: 2
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      run: site-analyzer
  template:
    metadata:
      labels:
        run: site-analyzer
    spec:
      containers:
      - envFrom:
        - secretRef:
            name: site-analyzer-secrets
        image: adomate/site-analyzer:8917c4742c8e5dba0edeaa3d6fefd4d6355c2d1a
        imagePullPolicy: Always
        name: site-analyzer
        ports:
        - containerPort: 3000
        resources:
          limits:
            cpu: 1000m
            memory: 512Mi
          requests:
            cpu: 200m
            memory: 50Mi
      imagePullSecrets:
      - name: dockerhub-credentials
