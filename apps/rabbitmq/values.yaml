rabbitmq:
  auth:
    username: adomate
    existingPasswordSecret: passwords
  primary:
    resources:
      requests:
        memory: 512Mi
        cpu: 50m
      limits:
        memory: 1024Mi
        cpu: 250m
  livenessProbe:
    enabled: false
